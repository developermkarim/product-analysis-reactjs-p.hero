import React from 'react';
// import useReviews from '../../Hooks/useReview';

const Review = ({perReviews}) => {
   console.log(perReviews);
    
    return (
        <div>
    
            
        </div>
    );
};

export default Review;